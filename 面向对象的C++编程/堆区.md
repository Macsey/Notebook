在堆区开辟数据：利用new关键字——返回开辟内存的地址

此处指针的本质也是局部变量，放在栈上，指针保存的数据放在堆区
在堆区释放数据：利用delete关键字

1. new 的基本用法
   ```cpp
int *p = new int(10) ;//创建一个数据
int *arr = new int[10] ;//创建一个数组
```
2. delete的基本用法
```cpp
delete [] arr;
delete p;
```
