交换两个数的另一种方法：注意只有定义了加减法的数据类型才能采用此方法
```cpp
#include<iostream>
using namespace std;
//交换两个数
int main()
{
	int a, b;
	cin >> a >> b;
	a = a + b;//a=a+b,b=b
    b = a - b;//a=a+b,a=b
    a = a - b;//a=b,b=a-b
    cout << a << " " << b << endl;
}
```